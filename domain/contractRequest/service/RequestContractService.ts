export class RequestContractService {
  // TODO: ただ生成するだけならFactoryとかでいいかも
  execute(contractorId: number, licensePlanId: number): boolean {
    // licenseplanを取得する
    // licensePlan情報を用いてContractRequestを生成する
    return true;
  }
}
